package io.github.bckfnn.mongodb;

public class Cursor {
    /*
     * item
 	 * add_options
 	 * batch_size
 	 * clone
 	 * close
 	 * count
 	 * distinct
 	 * each
 	 * explain
 	 * hint
 	 * limit
 	 * max_scan
 	 * next_object
 	 * remove_options
 	 * rewind
 	 * skip
 	 * sort
 	 * tail
 	 * to_list
 	 * where
 	 * alive
 	 * cursor_id
 	 * fetch_next
     */
}
